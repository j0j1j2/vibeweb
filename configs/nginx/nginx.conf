worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;

    server {
        listen 80;
        server_name _;

        set $tenant_id $http_x_tenant_id;

        if ($tenant_id = "") {
            return 400 '{"error": "missing tenant id"}';
        }

        # Validate tenant ID is a UUID — reject path traversal attempts
        if ($tenant_id !~ "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$") {
            return 400 '{"error": "invalid tenant id"}';
        }

        root /data/tenants/$tenant_id/public;

        # Block path traversal and symlink following
        disable_symlinks on;

        location ~ /\.\. {
            deny all;
            return 403;
        }

        location ~ /\.git {
            deny all;
            return 404;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options SAMEORIGIN always;
    }
}
